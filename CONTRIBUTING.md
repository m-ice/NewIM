# Contributing

Start from a task in `tasks/manifest.json` or create an approved task/ADR first. Keep changes inside declared module scope. Every PR should state behavior changed, compatibility/migration impact, validation run and observability impact.

Third-party code or copied snippets require a provenance record and license review before merge.
