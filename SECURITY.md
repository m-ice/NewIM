# Security policy

Do not open public issues containing exploitable vulnerabilities, secrets, tokens or private user data. Report security concerns to the project owner at `luckyice6@gmail.com` until a dedicated security address/process is published.

Security-sensitive areas include authentication/session ownership, media authorization, admin APIs, webhook signatures, rate limiting, push tokens, logs and dependency supply chain.
